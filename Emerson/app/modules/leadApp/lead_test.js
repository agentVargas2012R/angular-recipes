'use strict';

describe('emersonApp.leadApp module', function() {

  beforeEach(module('emersonApp.leadApp module'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('LeadCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});